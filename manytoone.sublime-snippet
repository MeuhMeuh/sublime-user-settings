<snippet>
	<content><![CDATA[
@ORM\ManyToOne(targetEntity="${1:Game}", inversedBy="${2:children}"${3:, fetch="EAGER"})
* @ORM\JoinColumn(name="${4:parentId}", referencedColumnName="${5:id}")
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>manytoone</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.php</scope>
</snippet>
